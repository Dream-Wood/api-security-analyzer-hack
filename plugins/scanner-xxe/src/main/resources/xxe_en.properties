# XXE Scanner English Localization
# Encoding: UTF-8

scanner.name=XXE Scanner
scanner.description=Detects XML External Entity (XXE) vulnerabilities

# Vulnerability Titles
vuln.body.title=XML External Entity (XXE) Injection
vuln.parameter.title=XML External Entity (XXE) Injection in Parameter

# Vulnerability Descriptions
vuln.body.description=The endpoint is vulnerable to XML External Entity (XXE) injection. The XML parser processes external entities without proper restrictions, allowing attackers to read arbitrary files from the server. Successfully accessed: {0} using XXE payload.

vuln.parameter.description=The endpoint is vulnerable to XXE injection through the ''{0}'' parameter. The application processes XML from query parameters without proper entity restrictions.

# Recommendations
vuln.recommendation1=Disable XML external entity processing in all XML parsers
vuln.recommendation2=Use less complex data formats like JSON when possible
vuln.recommendation3=Configure XML parsers to disable DTD processing
vuln.recommendation4=Implement input validation and whitelist acceptable XML schemas
vuln.recommendation5=Use up-to-date XML processors with secure defaults
vuln.recommendation6=Apply the principle of least privilege for file system access
vuln.recommendation7=Validate and sanitize all user inputs
vuln.recommendation8=Use JSON instead of XML for API parameters

# Reproduction Steps
vuln.body.repro.step1=Send {0} request to {1}
vuln.body.repro.step2=Set Content-Type header to: {0}
vuln.body.repro.step3=Include XXE payload in request body:\n{0}
vuln.body.repro.step4=Observe file content in response (target: {0})

vuln.parameter.repro.step1=Send request with XML in parameter ''{0}''
vuln.parameter.repro.step2=Observe file content in response

# Logging Messages
log.starting_scan=Starting XXE scan on: {0}
log.testing_xxe=Testing XXE vulnerability for: {0}
log.testing_payload=XXE: {0} ({1})
log.testing_parameter=XXE in parameter: {0}

status.scanning=Scanning for XXE vulnerabilities...
status.completed=XXE scan completed
